CONFIG_ZMK_KEYBOARD_NAME="SWEEP"

# Bluetooth Configuration
# Set to 3 for split keyboards: 2 host devices + 1 for the split connection
# This reduces memory usage and makes profile switching simpler
CONFIG_BT_MAX_CONN=3
CONFIG_BT_MAX_PAIRED=3

